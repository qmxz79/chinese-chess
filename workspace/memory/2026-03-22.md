
- 已继续推进《中国象棋微信小游戏》的在线匹配联调：`miniprogram/pages/game/game.js` 现在会根据房间状态同步 `state.mode / side / moveCount / status`，并新增 `broadcastMove()`、`handleSignalEnvelope()`、`applyRemoteMove()`、`syncBoardFromRoom()`，使走子、同步、提和/认输等信令事件能够真正驱动棋盘状态。
- 在线房间侧已开始把本地落子广播成 `match.move`，并在收到远端 `match.move` / `match.sync` 时回写棋盘与房间状态；悔棋和重开也补了状态回写。
- 本轮修改后 `npm test --prefix /home/gem/workspace/agent/workspace/chinese-chess-minigame` 仍然 16/16 通过。
- 下一步最重要的是补强在线对局的严格回合控制、双方身份校验，以及断线重连后的局面同步一致性。
- 文件修改偏好：以后尽量优先使用 `exec` 脚本方式修改文件，少用 `edit` 的精确文本替换，避免因旧文本不完全一致导致失败。


## 补充记录（2026-03-22 12:28）
- 在继续推进《中国象棋微信小游戏》在线联调时，`miniprogram/pages/game/game.js` 曾被误写坏过一次，随后确认该文件在当前工作区内没有可直接 `git checkout` 的追踪版本，恢复必须依赖会话日志或临时缓存。
- 已确认 `/tmp/gamejs_source.txt` 只包含 `game.js` 的前 402 行，后半截仍需从会话日志继续拼接；当前修复策略是先恢复到可编译状态，再做小步修改，避免再次整文件污染。
- 后续修改文件时，仍然优先使用 `exec` 脚本方式而不是 `edit`，因为精确文本替换对这个项目的大文件/长补丁太脆弱。
- 2026-03-22 12:42：`miniprogram/pages/game/game.js` 已重写恢复完成，在线信令与棋盘联动重新可用；`node --check` 通过，`npm test --prefix /home/gem/workspace/agent/workspace/chinese-chess-minigame` 仍为 16/16 全绿。
- 后续优先继续补强在线对局的严格回合控制、双方身份校验和断线重连同步一致性。

## 待办清单（2026-03-22 12:42）
- [重大进展] 补强《中国象棋微信小游戏》在线对局的严格回合控制：确保只有当前轮到的一方才能发子、发错直接拦截并提示。
- [重大进展] 补强双方身份/side 校验：连接建立、收包、落子、同步、认输/提和等流程都要校验房间身份，防止错配或串房。
- [重大进展] 完成断线重连同步一致性：恢复房间 snapshot 后自动对齐棋盘、回合号和 moveSeq，并验证本地历史清空/重建正确。
- [一般工作] 继续补 UI 交互细节：提示、日志、按钮状态和联机状态文案的可读性。
- [一般工作] 再跑一轮回归测试，确认 `game.js` 的重写没有破坏本地对局、结果页或协议转换。

### 重大进展判断标准
- 任意一项“重大进展”完成并验证通过，就算一次值得报告的进展。
- 一般工作完成只记录，不单独打扰汇报。
- 如果只是重复修补或小范围样式调整，不视为重大进展。
- 2026-03-22 14:38：`miniprogram/pages/game/game.js` 已进一步收紧在线对局逻辑：`isMyTurn()` 按房间 turn 判断，`commitMove()` 在在线模式下会拦截非回合方落子；`handleSignalEnvelope()` 也加入了基础的房间/来源过滤。`npm test --prefix /home/gem/workspace/agent/workspace/chinese-chess-minigame` 仍保持 16/16 通过。
